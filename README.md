# macos-e2e-kit

E2E-test any macOS app with an agent: an embeddable debug IPC bridge (Swift package), a zero-dependency Node driver/runner, and Claude Code skills for onboarding and running observe→act→verify loops.

Scaffold — implementation in progress.
